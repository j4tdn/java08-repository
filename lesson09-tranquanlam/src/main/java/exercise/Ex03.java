package exercise;

public class Ex03 {
	public static void main(String[] args) {
		// change location item
		// if item exist remove it
		// 
		permutation("xyz");
	}

	private static String[] permutation(String s) {
		String[] list = s.split("");
		// using matrix
		// get list from matrix
		// remove item is the same
		
		for (int i = 0; i < list.length; i++) {
			for (int j = 0; j < list.length; j++) {
				
			}
			
		}
		return list;
	}
}
