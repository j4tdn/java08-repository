<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>

	<session-factory>
		<!-- JDBC Database connection settings -->
		<property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
		<property name="connection.url">jdbc:mysql://localhost:3306/java08_hibernate_mapping?useSSL=false</property>
		<property name="connection.username">root</property>
		<property name="connection.password">1234</property>

		<!-- Select our SQL dialect engine, type of database-->
		<property name="dialect">org.hibernate.dialect.MySQL5Dialect</property>

		<!-- Echo the SQL to stdout -->
		<property name="show_sql">true</property>

		<!-- Drop and re-create the database schema on startup -->
		<!-- NOT USE
			create-drop
			+ Drop and re-create the database schema on startup
			update: 
			+ create table if not exists 
			+ alter table if something diff with db tables
		-->
		<!-- 
		<property name="hbm2ddl.auto">create-drop</property>
		-->

		<!-- Set the current session context -->
		<property name="current_session_context_class">thread</property>
		
		<!-- application has to handle modifications explicitly through transactions itself -->
        <!-- <property name="connection.autocommit">false</property>  -->
		
		<!-- persistence class -->
		
		<!-- 
		<mapping class="persistence.fkassociation.Employee" />
		<mapping class="persistence.fkassociation.Account" />
		 -->
		 
		<!--  
		<mapping class="persistence.jointable.Employee" />
		<mapping class="persistence.jointable.Account" />
		 -->
		 
		<mapping class="persistence.Employee" />
		<mapping class="persistence.Project" />
	</session-factory>

</hibernate-configuration>